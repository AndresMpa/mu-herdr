id = "muherdr.notify"
name = "MμHerdr Notify"
version = "0.1.0"
description = "System, Slack, and Telegram alerts when an agent needs you or finishes"
platforms = ["linux", "macos"]

[[events]]
on = "pane.agent_status_changed"
command = ["bash", "notify.sh"]
