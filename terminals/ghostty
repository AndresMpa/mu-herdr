# MμHerdr: send Shift-Space as a distinct chord (Kitty keyboard protocol).
# iTerm2 cannot do this. Put this in ~/.config/ghostty/config and run herdr in Ghostty.
# brew install --cask ghostty
keybind = shift+space=text:\x1b[32;2u
